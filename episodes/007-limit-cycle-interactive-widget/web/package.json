{
  "name": "bergner-limit-cycle-widget",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit && vite build && npm run verify:offline",
    "verify:offline": "node scripts/verify-offline-build.mjs"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.5",
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@types/plotly.js": "^3.0.10",
    "plotly.js-dist-min": "^3.7.0"
  }
}
